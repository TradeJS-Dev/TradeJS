# docker-compose.yml

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    restart: always
    depends_on:
      redis:
        condition: service_healthy
      timescale:
        condition: service_healthy
    image: aleksnick/inv-app:latest
    expose:
      - "3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./screenshots:/app/public/screenshots
      - ./.env:/app/.env:ro
    networks:
      - web
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  timescale:
    container_name: inv-timescale
    image: timescale/timescaledb:latest-pg16
    environment:
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=app
      - POSTGRES_DB=app
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql/create.sql:/docker-entrypoint-initdb.d/01-create.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped
    networks:
      - web

  redis:
    image: redis:alpine
    container_name: inv-redis
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - web

  redisinsight:
    image: redis/redisinsight:latest
    container_name: inv-redisinsight
    restart: unless-stopped
    depends_on:
      - redis
    expose:
      - "5540"
    environment:
      - RI_USER_EMAIL=${REDISINSIGHT_USER_EMAIL:-admin@example.com}
      - RI_USER_PASSWORD=${REDISINSIGHT_USER_PASSWORD:-admin}
      - RI_PROXY_PATH=/redisinsight
    volumes:
      - redisinsight_data:/data
    networks:
      - web

  nginx:
    image: nginx:stable-alpine
    container_name: nginx
    restart: always
    volumes:
      - ./nginx/nginx.conf.ssl:/etc/nginx/nginx.conf.ssl:ro
      - ./nginx/nginx.conf.nossl:/etc/nginx/nginx.conf.nossl:ro
      - ./certbot/conf:/etc/letsencrypt:ro
      - ./certbot/www:/var/www/certbot:ro
    entrypoint: >
      sh -c "[ -f /etc/letsencrypt/live/aleksnick01inv.fvds.ru/fullchain.pem ]
      && cp /etc/nginx/nginx.conf.ssl /etc/nginx/nginx.conf 
      || cp /etc/nginx/nginx.conf.nossl /etc/nginx/nginx.conf;
      nginx -g 'daemon off;'"
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - app
      - redisinsight
    networks:
      - web
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    entrypoint: >
      sh -c "certbot certonly --webroot --webroot-path=/var/www/certbot
      --email aleksnick01@gmail.com --agree-tos --no-eff-email
      -d aleksnick01inv.fvds.ru || true"
    networks:
      - web

  certbot-cron:
    image: certbot/certbot
    container_name: certbot-cron
    restart: on-failure
    depends_on:
      - certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    entrypoint: sh -c "while true; do certbot renew --webroot -w /var/www/certbot && sleep 12h; done"
    networks:
      - web
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  web:
    driver: bridge

volumes:
  pgdata:
  redisinsight_data:
