interface:
  display_name: 'Strategy Release'
  short_description: 'Release and diagnose TradeJS strategies'
  default_prompt: 'Use $strategy-release to audit prior strategy evidence, bridge stronger historical results, run three causal core rounds, then rescue up to three complete cadence-diverse diagnostic seeds even when they failed release eligibility before validating one core plus deterministic AI-gate composition.'
