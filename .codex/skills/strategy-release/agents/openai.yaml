interface:
  display_name: 'Strategy Release'
  short_description: 'Release and diagnose TradeJS strategies'
  default_prompt: 'Use $strategy-release to evaluate one TradeJS core plus deterministic AI-gate composition, rescue profitable raw direction cohorts before rejecting a gate, and diagnose live behavior with separate immutable logic and risk-scale evidence.'
